/*
var self = this;

function update(e){
    switch(self.getAnimName()){
        case "idle", "singUP":
            // PUSSY
        case "singUP":
            this.y = this.y - 100;
            this.x = this.x - 25;

        default:
            // PUSSY
    }
}

function postCreate(){
    switch(self.getAnimName()){
        case "idle", "singUP":
            this.scale.set(this.scale.x + .3, this.scale.y + .3);

        case "singUP":
            this.y = this.y - 100;
            this.x = this.x - 25;

        default:
            this.scale.set(this.scale.x, this.scale.y);
    }
}
*/